{
  "query": "{ products(first: 5) { nodes { __typename id slug name shortDescription ... on SimpleProduct { price regularPrice salePrice stockStatus stockQuantity image { sourceUrl altText } } ... on VariableProduct { price regularPrice salePrice stockStatus image { sourceUrl altText } } } } productCategories(first: 8) { nodes { id slug name description count image { sourceUrl } } } }"
}
